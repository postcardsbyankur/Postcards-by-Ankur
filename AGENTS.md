# Project Configuration
- The user's Firebase project is `postcardsbyankur-9e848`. DO NOT attempt to provision a new Firebase project or use `invertible-lexicon-tkm1r`.
- Do NOT push code changes to GitHub automatically. Only push to GitHub when explicitly requested by the user after they have reviewed and tested the changes.
